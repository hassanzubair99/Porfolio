// Temporary implementation without database
export const db = {
  // Add any methods your code uses here
  // For example:
  user: {
    findMany: async () => [],
    findUnique: async () => null,
    create: async () => ({}),
    update: async () => ({}),
    delete: async () => ({})
  }
} 